name: Deploy Simple App to Local Minikube

on:
  workflow_dispatch:
  push:
    branches:
    - main

jobs:
  fetch-env-from-repo-b:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repositório A
      uses: actions/checkout@v3

    - name: Checkout Repositório B
      uses: actions/checkout@v3
      with:
        repository: rogerioanselmo/devops-repo
        token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        path: node-api

    - name: Copiar .env do repositório B
      run: cp .env .env

    - name: Verificar variável de ambiente
      run: |
        export $(cat .env | xargs)
        echo "ENV_REPO: $ENV_REPO"
